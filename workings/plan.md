# Java AI Game
### Autonomous Computer-controlled Game Characters

## Fuzzy Logic
### Rules
1. IF `maze_end` IS `far` AND `ghost` IS `blue` THEN `player` IS `cold`
2. IF `maze_end` IS `near` AND `ghost` IS `orange` THEN `player` IS `warm`
3. IF `maze_end` IS `beside` AND `ghost` IS `red` THEN `player` IS `hot`
